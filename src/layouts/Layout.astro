---
import "../styles/global.css";
import "@fontsource/nunito";
import "@fontsource/inter";
import { siteConfig } from "../data/siteConfig";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
	title?: string;
	description?: string;
	image?: string;
	imageWidth?: number | string;
	imageHeight?: number | string;
}

const {
	title = siteConfig.title,
	description = siteConfig.description,
	image = "/og-image.png",
	imageWidth,
	imageHeight,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang={siteConfig.language}>
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/rosa-knopf-logo.png" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>

		<!-- Open Graph / Facebook -->
		<meta property="og:locale" content={siteConfig.language || "de"} />
		<meta property="og:type" content="website" />
		<meta property="og:url" content={Astro.url} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:site_name" content={siteConfig.title} />
		<meta
			property="og:logo"
			content={new URL(
				"/rosa-knopf-logo.png",
				Astro.site || siteConfig.siteUrl,
			).toString()}
		/>
		<meta
			property="og:image"
			content={new URL(
				image,
				Astro.site || siteConfig.siteUrl,
			).toString()}
		/>
		{
			imageWidth && (
				<meta property="og:image:width" content={String(imageWidth)} />
			)
		}
		{
			imageHeight && (
				<meta
					property="og:image:height"
					content={String(imageHeight)}
				/>
			)
		}

		<!-- Twitter -->
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={Astro.url} />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content={description} />
		<meta
			property="twitter:image"
			content={new URL(
				image,
				Astro.site || siteConfig.siteUrl,
			).toString()}
		/>
	</head>
	<body
		class="bg-gray-50 text-brand-text min-h-screen flex flex-col font-sans"
	>
		{
			siteConfig.announcement?.enabled && (
				<div class="bg-gray-100 text-gray-800 px-4 py-2 text-center text-sm font-medium border-b border-gray-200">
					{siteConfig.announcement.message}
				</div>
			)
		}
		{
			siteConfig.holidayMode.enabled && (
				<div class="bg-rose-500 text-white text-center py-2 px-4 shadow sticky top-0 z-50">
					<p class="text-sm font-medium">
						🌴 {siteConfig.holidayMode.message}
					</p>
				</div>
			)
		}

		<Header />

		<main class="flex-grow container mx-auto px-4 py-8">
			<slot />
		</main>

		<Footer />

		<script
			is:inline
			src="https://identity.netlify.com/v1/netlify-identity-widget.js"
		></script>
		<script is:inline>
			if (window.netlifyIdentity) {
				window.netlifyIdentity.on("init", (user) => {
					if (!user) {
						window.netlifyIdentity.on("login", () => {
							document.location.href = "/admin/";
						});
					}
				});
			}
		</script>
	</body>
</html>
